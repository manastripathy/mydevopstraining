pipeline {
	agent any
	stages {
		stage('Build application'){
			steps {
				sh 'mvn -f mydevopstraining/pom.xml clean package'
			}
			post {
				success {
					echo "Now archiving the artifacts..."
					archiveArtifacts artifacts: '**/*.war'
				}
			}
		}			
	}
}